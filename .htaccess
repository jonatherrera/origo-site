Options -Indexes

# Force HTTPS
RewriteEngine On
RewriteCond %{HTTPS} off
RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [R=301,L]

# Force non-www (canonical: origo.ooo)
RewriteCond %{HTTP_HOST} ^www\.origo\.ooo$ [NC]
RewriteRule ^ https://origo.ooo%{REQUEST_URI} [R=301,L]
